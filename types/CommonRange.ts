export type CommonRangeData = {
  val?: string,
  key: string,
  min: string | number,
  max: string | number,
}

export type CommonRangeOptions = {
  label?: string,
  key?: string,
  toggleDeactivate?: boolean,
  toggleVisible?: boolean,
  toggleVisibleStatus?: boolean,
}
