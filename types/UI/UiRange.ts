export type UiRangeData = {
  val?: string,
  key: string,
  min: string | number,
  max: string | number,
}

export type UiRangeOptions = {
  label?: string,
  key?: string,
  toggleDeactivate?: boolean,
  toggleVisible?: boolean,
  toggleVisibleStatus?: boolean,
}
